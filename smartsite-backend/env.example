# Copier vers .env (non versionné) ou utiliser pour docker --env-file
# Vide (hors NODE_ENV=production) : défaut code → mongodb://127.0.0.1:27017/smartsite (voir app.module.ts)
# MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster.mongodb.net/smartsite?retryWrites=true&w=majority
MONGODB_URI=
